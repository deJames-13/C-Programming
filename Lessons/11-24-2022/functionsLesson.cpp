#include <iostream>
using namespace std;

int main()
{
    cout << "FUNCTIOND / METHODS / SUB-ROUTINES" << endl;
    // the modular creation of programs

    /*
    TYPES of METHODs
        - Function
            > a method or sub-routine that does return a single data value
            > return methods
        FORMAT / SYNTAX
            - NON PARAMETERIZED > no parameters (passed values)


            [data type] functionName(){
                // code

                return (value of same data type. Ex. int main() will return 0)
            }
            Example:
                int Add(){
                    return 0;
                }


            - PARAMETERIZED > has parameters / values to passed onto the function

            [data type] functionName(type parameter1, type parameter2){
                // code

                return (value of same data type. Ex. int main() will return 0)
            }
            Example:
                int Add(int arg1, int arg2){
                    return 0;
                }

                Passing of Parameter
                2 types
                a. Formal Params - receives data value from formal params
                b. Actual Params - parameters after the caller name


        - Procedure

     */

    return 0;
}